🌿 Sembralas — Base Técnica v0.2

Estado actual del proyecto (Astro + Tailwind v4)
Actualización posterior a implementación de Hero Premium + Sistema Reveal

1. Stack Tecnológico
Core

Astro v5

TailwindCSS v4

Vite

TypeScript

Prettier

Enfoque Arquitectónico

One-page narrativa

Separación estricta entre:

UI (componentes reutilizables)

Layout

Contenido editorial (Markdown futuro)

Data estructurada (JSON futuro)

Performance-first

Sin dependencias innecesarias

2. Arquitectura del Proyecto
SembraAlas/
│
├── public/
│   ├── favicon.ico
│   ├── favicon.svg
│   └── images/
│       └── hero-test.jpg
│
├── src/
│   ├── components/
│   │   ├── ui/
│   │   │   ├── Button.astro
│   │   │   ├── Card.astro
│   │   │   └── SectionTitle.astro
│   │   │
│   │   └── sections/
│   │       ├── Hero.astro
│   │       ├── Historia.astro
│   │       ├── Impacto.astro
│   │       ├── Jornadas.astro
│   │       ├── Premios.astro
│   │       ├── Galeria.astro
│   │       └── Apoyar.astro
│   │
│   ├── layouts/
│   │   └── BaseLayout.astro
│   │
│   ├── styles/
│   │   ├── tokens.css
│   │   └── globals.css
│   │
│   └── pages/
│       └── index.astro
│
└── project-docs/
    └── sembralas-base-tecnica-v0.2.md
3. Hero Premium — Implementación v0.2
Objetivo

Crear una experiencia editorial tipo Patagonia:

Pantalla completa (100svh)

Sticky hold (200vh total)

Parallax leve

Overlay profesional

Sin librerías externas

Estructura Técnica
Sistema Sticky

.hero-shell → altura 200vh

.hero-sticky → sticky 100vh

.hero-bg → fondo absolute

.hero-content → capa superior

.hero-scroll → indicador inferior

Hero.astro (v0.2)
---
import Button from "../ui/Button.astro";
---

<section id="hero" class="relative">
  <div class="hero-shell">
    <div class="hero-sticky">
      <div id="heroBg" class="hero-bg" aria-hidden="true"></div>

      <div class="container hero-content">
        <div class="max-w-[70ch]">
          <p class="text-sm tracking-[0.2em] uppercase opacity-80">
            Reforestación con respaldo académico
          </p>

          <h1 class="text-5xl md:text-6xl mt-4">
            Sembrar hoy. Respirar mañana.
          </h1>

          <p class="mt-6 max-w-[60ch] text-lg opacity-90">
            Proyecto real, medible y documentado para restaurar ecosistemas con impacto verificable.
          </p>

          <div class="mt-8 flex flex-wrap gap-4">
            <Button href="#historia">Conoce el proyecto</Button>
            <Button href="#apoyar" variant="secondary">
              Apoya la reforestación
            </Button>
          </div>
        </div>
      </div>

      <a href="#historia" class="hero-scroll" aria-label="Ir a Historia">
        <span>Scroll</span>
        <i></i>
      </a>
    </div>
  </div>
</section>
CSS Hero (globals.css)
.hero-shell {
  position: relative;
  height: 200vh;
}

.hero-sticky {
  position: sticky;
  top: 0;
  height: 100vh;
  overflow: hidden;
}

.hero-bg {
  position: absolute;
  inset: 0;
  background-image: url("/images/hero-test.jpg");
  background-size: cover;
  background-position: center;
  z-index: 0;
  will-change: transform;
}

.hero-bg::after {
  content: "";
  position: absolute;
  inset: 0;
  background: linear-gradient(
    to bottom,
    rgba(0,0,0,0.35),
    rgba(0,0,0,0.15) 40%,
    rgba(0,0,0,0.45)
  );
}

.hero-content {
  position: relative;
  z-index: 2;
  height: 100vh;
  display: flex;
  align-items: center;
  color: white;
}
Parallax Script (BaseLayout.astro)

Ubicado al final del <body>:

<script is:inline>
(() => {
  const bg = document.getElementById("heroBg");
  if (!bg) return;

  const onScroll = () => {
    const y = window.scrollY || 0;
    const offset = Math.min(40, y * 0.12);
    bg.style.transform = `translateY(${offset}px) scale(1.05)`;
  };

  onScroll();
  window.addEventListener("scroll", onScroll, { passive: true });
})();
</script>
4. Sistema Reveal (Fade-in Global)

IntersectionObserver

Clase .reveal

Activación con .is-visible

Sin librerías externas

Compatible con reduce motion

Estado: ✅ Implementado

5. Estado General del Roadmap
Fase 1 — Landing Narrativa

Hero premium sticky + parallax → ✅

Secciones base estructuradas → ✅

Animaciones reveal → ✅

Componentes UI reutilizables → ✅

Estado: 85% completado

Pendiente:

Imagen AVIF optimizada

Lazy loading en galería

Medición Lighthouse

Fase 2 — Catálogo sin pagos

No iniciada.

Fase 3 — Ecommerce (Wompi)

No iniciada.

Fase 4 — Panel Administrativo

No iniciada.

6. Performance Status
Requisito	Estado
Sin librerías pesadas	✅
Hero optimizado AVIF	❌
Lazy loading imágenes	❌
Preload hero	❌
Lighthouse > 90	🔄 No medido
7. Principios Aplicados

Menos dependencias.

Más control nativo.

Animación sutil.

Diseño editorial.

Estructura modular.

Performance primero.

8. Próximo Paso Estratégico

Opciones prioritarias:

Optimización real del hero (AVIF + preload correcto).

Sección Impacto con contador animado.

Pulir transición hero → historia.

Implementar sistema de datos estructurados para impacto real.

Estado Oficial

Sembralas v0.2
Arquitectura sólida
Hero premium implementado
Base lista para escalar

